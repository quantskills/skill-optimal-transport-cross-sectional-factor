# Source Boundary

Use public sources or user-provided snapshots only. Direct `panda_data` queries require explicit authorization and must preserve method, parameters, fields, date range, returned schema, and availability evidence. Do not store credentials, tokens, private messages, or unredacted sensitive data.

Synthetic fixtures are mechanics tests only. They are never live PandaData evidence.
