# Portable Loader Prompt

Replace `<OPTIMAL_TRANSPORT_CROSS_SECTIONAL_FACTOR_SKILL_ROOT>` with the repository root, then load:

1. `<OPTIMAL_TRANSPORT_CROSS_SECTIONAL_FACTOR_SKILL_ROOT>/SKILL.md`;
2. the required references under `<OPTIMAL_TRANSPORT_CROSS_SECTIONAL_FACTOR_SKILL_ROOT>/references/`;
3. schemas under `<OPTIMAL_TRANSPORT_CROSS_SECTIONAL_FACTOR_SKILL_ROOT>/schemas/`;
4. deterministic scripts under `<OPTIMAL_TRANSPORT_CROSS_SECTIONAL_FACTOR_SKILL_ROOT>/scripts/`.

Use only strictly earlier dates for reference distributions. Require authorization before direct `panda_data` access, external evaluation, or writes. Do not dynamically change feature weights, train black-box models, handle credentials, use private data, or provide investment advice.
